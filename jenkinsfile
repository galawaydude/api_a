// Jenkinsfile
pipeline {
    agent any

    stages {
        // ## Stage 1: Get the code
        stage('Checkout Code') {
            steps {
                // Your GitHub URL has been added below.
                // Make sure 'main' is the correct name for your branch.
                git branch: 'main', url: 'https://github.com/galawaydude/api_a.git'
            }
        }

        // ## Stage 2: Analyze code quality
        stage('SonarQube Code Analysis') {
            steps {
                script {
                    def sqScanner = tool 'DefaultSonarScanner'
                    // Your token has been added to the command below
                    sh "${sqScanner}/bin/sonar-scanner -Dsonar.login=squ_1f323da6b3d5d73c0dac682fac096ef7c85f7213"
                }
            }
        }

        // ## Stage 3: Build and Test the API
        stage('Build and Test') {
            steps {
                script {
                    def dockerImage = docker.build("llm-api:${env.BUILD_NUMBER}")

                    dockerImage.withRun { container ->
                        sh 'sleep 15'
                        sh 'npm install -g newman'
                        sh "newman run api_tests.postman_collection.json"
                    }
                }
            }
        }
    }
}